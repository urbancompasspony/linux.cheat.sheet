For Ubuntu 22.04 LTS and Raspberry Pi 3

sudo dd if=/dev/zero of=/swapfile bs=1k count=2000000
sudo chmd 0600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo "/swapfile none swap sw 0 0" | sudo tee -a /etc/fstab

sudo apt install network-manager git linux-headers-generic build-essential dkms linux-headers-raspi firmware-ath9k-htc -y

sudo apt purge dnsmasq-base -y; sudo killall dnsmasq
sudo systemctl stop systemd-resolved; sudo systemctl disable systemd-resolved; sudo systemctl mask systemd-resolved; sudo unlink /etc/resolv.conf

sudo nano /etc/resolv.conf
nameserver 1.0.0.1
nameserver 127.0.0.1

sudo nano /etc/sysctl.conf
net.ipv4.ip_forward=1

Driver wifi above on apt install!
